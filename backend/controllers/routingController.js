// routingController.js — RETIRED
// This controller's /api/routing/generate endpoint was returning a flat list of
// road segments (not a point-to-point route) and was never called by the client.
// Real routing is handled by routes/routing.js → GET /api/pg-routing/route
// This file is kept as a tombstone; the route is no longer registered in server.js.
